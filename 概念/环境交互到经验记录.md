---
title: 环境交互到经验记录
aliases:
  - transition record
  - 环境转移记录
tags:
  - knowledge-graph/core
  - reinforcement-learning/environment
  - reinforcement-learning/dqn
status: learning
created: 2026-09-03
updated: 2026-09-03
related:
  - "[[概念/标准环境接口]]"
  - "[[概念/经验回放]]"
  - "[[概念/观察与动作]]"
  - "[[059-cartpole-step-to-transition]]"
---

# 环境交互到经验记录

一条环境转移记录把同一个交互步的前因和后果保存在一起：动作前观察、实际动作、动作后奖励、动作后观察，以及两个结束标记。

```text
当前 observation
→ 执行 action
→ env.step(action)
→ reward、next_observation、terminated、truncated
→ 组成一条 transition
```

## 时间关系

若本步的旧观察记作 $o_t$，执行动作后得到 $o_{t+1}$：

$$
T_t=(o_t,a_t,r_{t+1},o_{t+1},terminated,truncated)
$$

保存完成后才执行 `observation = next_observation`。因此相邻两条经验必须满足：前一条的 `next_observation` 等于后一条的 `observation`。

## 职责边界

- `reset()` 提供本局第一项当前观察；
- 策略提供实际执行动作；
- `step()` 根据动作推进环境并产生后果；
- `VectorTransition` 只保存数据，不计算 Q 值；
- 回放缓冲区只保存和抽样经验，不替环境产生经验；
- 只有后续 loss、`backward()` 和 optimizer 才会修改网络参数。

## 对应课程与代码

- [[037-cartpole-interface-random-baseline|CartPole 环境接口与随机基线]]
- [[059-cartpole-step-to-transition|CartPole 一步交互怎样变成回放经验]]
- `exercises/cartpole_step_to_transition.py`

> [!warning] 当前边界
> 学习者已验证三步真实 CartPole 数据能形成连续经验，并能在终止后立即停止交互；还没有把这些经验用于 CartPole 网络训练。
