---
title: 共享参数与预测联动
aliases:
  - shared parameters
  - prediction coupling
tags:
  - knowledge-graph/core
  - reinforcement-learning/neural-network
  - reinforcement-learning/dqn
status: learned
created: 2026-09-04
updated: 2026-09-12
related:
  - "[[概念/神经网络参数与预测]]"
  - "[[概念/优化器与参数更新]]"
  - "[[概念/ReLU与梯度门控]]"
  - "[[概念/经验回放]]"
  - "[[063-one-update-affects-many-predictions]]"
---

# 共享参数与预测联动

神经网络不是为每条观察保存一套独立参数。不同观察调用同一个模型时，都会使用同一组权重和偏置。

```text
观察 A ─┐
观察 B ─┼→ 同一组网络参数 → 各自的 Q 值
观察 C ─┘
```

一条训练经验只负责产生本轮 loss 和梯度，但 optimizer 修改的是共享参数。修改完成后，其他没有进入本轮 loss 的观察再次前向时也会读取新参数，因此预测可能变化。

## 直接变化与间接变化

- 实际动作的输出层行直接连接 loss，因此本轮可以收到梯度并更新；
- 未选动作的输出层行可能保持不变；
- 但所有动作输出都读取共享隐藏表示，所以隐藏层改变后，未选动作 Q 值仍可能间接变化；
- 没参加训练的观察不在本轮计算图里，但下一次前向会使用更新后的共享参数。

## 与 Q 表的区别

Q 表可以只改一个明确格子 `Q(s,a)`，其他格子保持不变。神经网络修改的是一个共享函数的参数，一处训练通常会影响一片输入区域。

这种联动让网络可能把经验推广到相似观察，也带来干扰其他预测的风险。不能因为当前样本 loss 下降，就推断所有观察都变得更准确。

## 一个参数就能看到联动

设模型为 q = w × x，初始 w = 0。只拿 A 训练：x_A = 1，目标为 1，平方损失为 1，对 w 的梯度为 2 × (0 - 1) × 1 = -2。普通 SGD 学习率 0.1，把 w 更新成 0.2。

再次前向时，A 的预测从 0 变成 0.2；没有参与本次损失的 B，若 x_B = -1，预测也会从 0 变成 -0.2。B 不必参与反向就能受影响，因为下一次读取的是同一个新 w。如果 B 的目标也是 1，这次对 A 有利的更新反而让 B 更远离目标。

## 对应课程与代码

- [[063-one-update-affects-many-predictions|一次更新为什么会改变多个观察的 Q 值]]
- `exercises/cartpole_shared_parameter_update.py`

> [!info] 第 063 课的证据范围
> 学习者当时确认了一条固定观察的 SGD 更新和多个预测联动。该练习本身没有测量真实 CartPole 回报，不能替代完整训练的评估记录。
