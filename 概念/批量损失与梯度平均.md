---
title: 批量损失与梯度平均
aliases:
  - Batch mean loss
  - 批量损失归约
tags:
  - reinforcement-learning/neural-network
  - reinforcement-learning/pytorch
  - reinforcement-learning/dqn
status: learned
created: 2026-09-02
updated: 2026-09-02
related:
  - "[[概念/批量张量与逐行动作索引]]"
  - "[[概念/自动求导与梯度]]"
  - "[[概念/优化器与参数更新]]"
  - "[[概念/DQN训练流程]]"
  - "[[051-pytorch-batch-loss-update]]"
  - "[[概念/终止掩码与批量DQN目标]]"
  - "[[064-mean-loss-and-single-sample-tradeoff]]"
---

# 批量损失与梯度平均

一批经验中的每条经验先用自己的 selected Q 和 target 计算逐条损失，再对这些损失取平均，形成一个标量 loss。

```text
每条经验的预测与 target
→ 每条经验自己的平方损失
→ mean 得到标量 loss
→ backward 汇总平均梯度
→ optimizer 对整批只更新一次
```

## 为什么经验不会因平均而消失

平均 loss 的计算图仍分别连接每条经验。`backward()` 会把每条路径的梯度除以批量大小后相加到共享参数 `.grad`，因此每条经验都保留自己的方向和大小贡献。

## 为什么通常不用求和

求和损失会让梯度规模随着批量条数直接增大。平均损失让更新尺度对 batch size 更稳定，使学习率不会仅因批量条数变化就改变含义。

## 对应课程与代码

- [[051-pytorch-batch-loss-update|一批误差怎样合成一次参数更新]]
- `exercises/pytorch_batch_loss_update.py`

学习者已经完成批量平均损失更新练习：两条经验各自保留损失路径，平均后共同写入共享参数梯度，optimizer 对整批只更新一次。当前尚未验证批量 target 或完整 DQN 训练。

## 平均改善不等于逐条改善

不同经验的梯度会在共享参数上相加。它们可能方向一致，也可能互相冲突。optimizer 直接优化的是整批平均 loss，因此更新后平均值可以下降，同时某一条经验的 loss 暂时上升。

[[064-mean-loss-and-single-sample-tradeoff|第 064 课]]用两条固定经验展示：A 的 loss 从 `0.0289` 升到约 `0.0650`，B 从 `1.0000` 降到约 `0.6950`，平均值仍从 `0.51445` 降到约 `0.38001`。
