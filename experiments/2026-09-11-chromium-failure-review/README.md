# Chromium 代表性失败诊断

## 问题、假设与配置

问题：接近中位成绩的一局，策略实际如何移动、得分和损命？假设：可能偏好少数动作并长时间贴边，未形成可靠生存行为。这是已有评测的观测性复盘，不是改奖励的因果实验。

原生游戏、CPU、单实例，零训练更新；seed209，task-v1，62维观察、18动作、每动作5 tick、250决策上限；使用首训train-06fbbc3a5a1c4ff78afc7ffabb1586c1/policy.pt。版本seeded-reset-v3；权重及评测来源见同日chromium-model-vs-random记录。只新增记录，不改变策略。

## 完整命令

从仓库根目录运行教师诊断：

```bash
.venv/bin/python experiments/2026-09-11-chromium-failure-review/inspect_episode.py
```

学习者可用已有GUI查看（本次未由教师观察GUI）：

```bash
.venv/bin/python exercises/chromium_dqn/play.py --run --seed 209
```

## 产物与结果

`exercises/chromium_dqn/runs/failure-seed209.json`保存summary与逐步steps，不提交生成数据。原生复现218决策、21.8模拟秒、5075分、hero_dead，与原评测一致。

第3步到达底边。动作11向下开火152步，全部含开火动作156步。204步结束位置满足abs(x)>=9.5或abs(y)>=7（世界坐标由观察还原），占93.6%。此阈值仅为描述边缘位置，不是危险区域定义。

85、92、134、159、218步各发生1次可见生命计数下降，当步reward均0。118、144、163步得分奖励分别25、25、0.75。没有记录具体碰撞对象，不能将单次得分或损命归因于特定敌人。

## 结论与下一步

该局动作有明显偏好，长期贴边，最终死亡；纯得分即时奖励未直接惩罚这些损命步骤。不能据此证明边缘导致死亡、奖励是唯一根因或所有种子行为一致。仅500次训练更新也是候选解释。

093先理解证据与假设的区别；094优先保持其他条件，检验增加训练预算的效果。尚未开展改进训练，没有新策略能力结论。
